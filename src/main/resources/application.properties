# SERVER CONFIGURATION
server.port=8080
spring.application.name=book-partner-portal
server.address=0.0.0.0

# DATABASE CONFIGURATION (Localhost)
spring.datasource.url=jdbc:mysql://localhost:3306/pubs?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA & HIBERNATE CONFIGURATION
# 'update' ensures it maps to your existing tables without dropping them
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# THYMELEAF CACHE (Set to false so HTML updates without restarting the server)
spring.thymeleaf.cache=false